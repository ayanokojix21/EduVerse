# ── Web framework ──────────────────────────────────────────────────────────────
fastapi[standard]==0.135.3
pydantic-settings==2.13.1
python-dotenv==1.2.2
python-multipart>=0.0.9

# ── Database (Local MongoDB) ───────────────────────────────────────────────────
motor==3.7.1
pymongo>=4.10,<4.16

# ── Auth ───────────────────────────────────────────────────────────────────────
python-jose[cryptography]==3.5.0
cryptography==46.0.7

# ── Resilience + async ──────────────────────────────────────────────────────────
tenacity==9.1.4
httpx==0.28.1
anyio>=4.0

# ── Google APIs (OAuth + Classroom sync — optional, when online) ───────────────
google-auth==2.49.1
google-api-python-client==2.194.0

# ── LangChain ecosystem ────────────────────────────────────────────────────────
# Core orchestration and local vector search
langchain==1.2.15
langchain-core==1.3.0
langchain-text-splitters>=0.3.11
langchain-community>=0.3.0
langchain-mongodb==0.7.2
langsmith>=0.3.45

# ── LangGraph ──────────────────────────────────────────────────────────────────
langgraph==1.1.7
langgraph-checkpoint-mongodb>=0.1.0

# ── Google Classroom Loader ────────────────────────────────────────────────────
langchain-google-classroom==0.1.2

# ── Cloud Teacher — DPO Distillation Only (never on live path) ─────────────────
langchain-google-genai==2.1.12

# ── Local Inference (Ollama — Gemma 4) ─────────────────────────────────────────
langchain-ollama>=0.3.0

# ── Local RAG: Embeddings + Reranking (100% Offline) ──────────────────────────
fastembed>=0.5.1
flashrank>=0.2.9

# ── PDF processing & Vision ──────────────────────────────────────────────────
pymupdf>=1.23.0
pymupdf4llm>=0.0.17

# ── Reinforcement Learning & Kaggle Orchestration ─────────────────────────────
gymnasium==1.1.1
numpy>=1.24.0
kaggle>=1.6.14

# ── Utilities ──────────────────────────────────────────────────────────────────
tiktoken>=0.7.0

# ── Testing ────────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.0.0